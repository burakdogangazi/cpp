#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}

/*
 * Preprocesseor directrives means it that processes your source code before the compiler sees it.
 * ''#'' means directives
 * The c++ preprocessor does not understand c++ it simply follows the preprocessor directives and gets the source code ready for the compiler
 * 90 keyword in c++, java has 50, c has 32 python has 32
 */
